#!/usr/bin/env bash

# This script generates native MacOS spelling and VSCode spelling configs
# so that tools across the MacOs toolchain can share a single source
# of truth for custom spellings. It takes a file

if [[ ! -f ~/.bin/update-spellcheck.conf ]]; then
  echo "Hello world"
fi
